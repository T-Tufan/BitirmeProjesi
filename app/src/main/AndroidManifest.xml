<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tezbalang">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:value="AIzaSyDKWqOlhNuJoeRZz2qLOCuLikyek8W0odo">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDKWqOlhNuJoeRZz2qLOCuLikyek8W0odo" />

        <activity
            android:name=".Anasayfa.AppPages.MrktKonumActivity"
            android:label="@string/title_activity_mrkt_konum" />

        <activity android:name=".Anasayfa.AppPages.MrktActivity" />
        <activity android:name=".Anasayfa.AppPages.FotoTanimaActivity" />
        <activity android:name=".Anasayfa.AppPages.BarkodOkumaActivity" />
        <activity android:name=".Anasayfa.AppPages.TumUrunlerActivity" />
        <activity android:name=".Anasayfa.AppPages.UrunDetayActivity" />
        <activity android:name=".Anasayfa.AppPages.KategoriIcerikActivity" />
        <activity android:name=".Anasayfa.AppPages.KategorilerActivity" />
        <activity android:name=".Anasayfa.AppPages.AnasayfaActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>